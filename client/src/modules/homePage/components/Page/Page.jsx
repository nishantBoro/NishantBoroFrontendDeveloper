import React from 'react';
import PropTypes from 'prop-types';

import { SearchBarContainer } from '../SearchBar';

function Page(props) {
  const {
    
  } = props;
  
  return (
    <div>
      <SearchBarContainer />
    </div>
  )
}

Page.propTypes = {
};

export default Page;
