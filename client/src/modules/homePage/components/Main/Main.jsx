import React from 'react';
import PropTypes from 'prop-types';

function Main(props) {
  const {
    
  } = props;
  
  return (
    <div>
      Test
    </div>
  )
}
Main.propTypes = {
};

export default Main;
