import React from 'react';
import PropTypes from 'prop-types';

function Main(props) {
  const {
    
  } = props;
  
  return (
    <div>
      Test
    </div>
  )
}
Main.propTypes = {
  str: PropTypes.string
};

export default Main;
